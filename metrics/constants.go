package metrics

const (
	// test server
	testHost = "127.0.0.1"
	testPort = ":9999"

	// endpoints
	home             = "/"
	metrics          = "/metrics"
	errorNotFound    = "/error-not-found"
	add              = "/add-connection"
	sub              = "/sub-connection"
	responseDuration = "/response-duration"

	// general
	system = "example"
)
