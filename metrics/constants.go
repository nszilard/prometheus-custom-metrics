package metrics

const (
	// server
	host = "127.0.0.1"
	port = "9988"

	// endpoints
	home             = "/"
	metrics          = "/metrics"
	errorNotFound    = "/error-not-found"
	add              = "/add-connection"
	sub              = "/sub-connection"
	responseDuration = "/response-duration"
)
